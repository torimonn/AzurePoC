# ローカル画面確認用。実値をGitへ登録しないでください。
AUTH_MODE=temporary
DEMO_MODE=false
TEMP_LOGIN_CODE=replace-with-a-local-temporary-code

# FastAPI連携。モックではtrue、本番接続時はfalseに変更します。
MOCK_MODE=true
FASTAPI_BASE_URL=http://fastapi:8000

# 将来切替: temporary / onprem / hybrid / entra
# AUTH_MODE=onprem
